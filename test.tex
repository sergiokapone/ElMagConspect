\documentclass[]{ConspectBook}
\let\enquote\emph

\newcommand{\includechapter}[1]{\input{#1/#1}}

\newcommand{\includetikz}[2]{\input{#1/tikz/#2.tikz}}


\begin{document}

%\includetikz{EMWaves}{total-reflection-geometry}

\includechapter{EMWaves}

%%% --------------------------------------------------------
%\subsection*{Введення ротора через циркуляцію}
%%% --------------------------------------------------------
%
%Виділимо в площині нескінченно малий прямокутний контур зі
%сторонами $dx$, $dy$, що паралельні до координатних осей (рис.~\ref{tikz:rot}).
%
%\begin{SCfigure}[][h!]
%	\centering
%
%	\includetikz{SteadyEfield}{rot}
%
%	\caption{До введення ротора через циркуляцію}
%	\label{tikz:rot}
%\end{SCfigure}
%
%Обчислимо циркуляцію вектора $\Efield$ вздовж контуру $1\to2\to3\to4\to1$
%проти годинникової стрілки.
%
%На стороні $1$--$2$ (нижня, обхід у позитивному напрямку осі $x$) дотична
%складова поля дає внесок $E_x(x,y)\,dx$. На протилежній стороні $3$--$4$
%(верхня) обхід іде у від'ємному напрямку осі $x$, тому внесок дорівнює
%$-E_x(x, y+dy)\,dx$. Сума цих двох внесків:
%\begin{equation*}
%	-\left[E_x(x, y+dy) - E_x(x,y) \right] dx = -\left( \frac{\partial
%		E_x}{\partial y} dy\right) dx = -\frac{\partial
%		E_x}{\partial y} dS.
%\end{equation*}
%Тут використано формулу \eqref{eq:sc_diff}, де $f(y) = E_x(x,y)$, а $\epsilon_y = dy$,
%а $dS = dx\,dy$ --- площа контуру.
%
%Аналогічно, на стороні $2$--$3$ (права, обхід у позитивному напрямку осі
%$y$) внесок дорівнює $E_y(x+dx, y)\,dy$, а на стороні $4$--$1$ (ліва, обхід
%у від'ємному напрямку осі $y$) внесок дорівнює $-E_y(x,y)\,dy$. Сума:
%\begin{equation*}
%	\left[E_y(x+dx, y) - E_y(x,y) \right] dy = \left( \frac{\partial
%		E_y}{\partial x} dx\right) dy = \frac{\partial
%		E_y}{\partial x} dS.
%\end{equation*}
%
%Повна циркуляція вздовж усього контуру:
%\begin{equation*}
%	d\Gamma = \oint \Efield \cdot d\vect{l} = \left( \frac{\partial
%		E_y}{\partial x} - \frac{\partial E_x}{\partial y} \right) dS =
%	(\rot\Efield)_z\, dS.
%\end{equation*}
%
%Величину $(\rot\Efield)_z$ можна розглядати як $z$-компоненту вектора
%$\rot\Efield$ (ротора вектора $\Efield$), який визначається як границя
%відношення циркуляції вектора вздовж малого замкненого контуру до площі
%цього контуру, коли нормаль до контуру $\vec n$ напрямлена вздовж
%відповідної осі (тут --- вздовж осі $z$, за правилом правого гвинта
%відносно напрямку обходу):
%\begin{equation*}
%	(\rot\Efield)_n = \lim_{dS \to 0} \frac{1}{dS}\oint \Efield \cdot d\vect{l}.
%\end{equation*}
%
%Проводячи аналогічні міркування для контурів у площинах $yz$ та $zx$,
%отримаємо решту компонент, і в результаті повний вираз для ротора у
%декартових координатах:
%\begin{equation}
%	\rot\Efield = \left( \frac{\partial E_z}{\partial y} - \frac{\partial
%		E_y}{\partial z} \right)\vect{i} + \left( \frac{\partial
%		E_x}{\partial z} - \frac{\partial E_z}{\partial x} \right)\vect{j} +
%	\left( \frac{\partial E_y}{\partial x} - \frac{\partial E_x}{\partial y}
%	\right)\vect{k}.
%\end{equation}
%
%\begin{Attention}
%	Ротор вектора --- це вектор!
%\end{Attention}

%% capacitor-angular-momentum.tikz
%% Заряджений циліндричний конденсатор в аксіальному полі B:
%% радіальне поле E, наскрізне B, циркулюючий вектор Пойнтінга Π.
%\definecolor{pfield}{rgb}{0,0.55,0.15}   % колір вектора Пойнтінга Π на рисунку
%
%\begin{tikzpicture}[>=Latex, thick]
%	% --- параметри рисунка ---
%	\pgfmathsetmacro{\R}{1.6}     % радіус циліндра
%	\pgfmathsetmacro{\Hc}{3.2}    % висота циліндра
%	\pgfmathsetmacro{\k}{0.32}    % стиснення еліпса (перспектива)
%	\pgfmathsetmacro{\Rk}{\R*\k}
%
%	% --- вісь обертання ---
%	\draw[gray,dashed] (0,-0.9) -- (0,\Hc+1.3);
%
%	% --- нижній еліпс: задня (прихована) дуга -- пунктир ---
%	\draw[thick,dashed] (0:{\R} and {\Rk}) arc (0:180:{\R} and {\Rk});
%	% --- нижній еліпс: передня (видима) дуга -- суцільна ---
%	\draw[thick] (180:{\R} and {\Rk}) arc (180:360:{\R} and {\Rk});
%	% --- верхній еліпс (повністю видимий) ---
%	\begin{scope}[yshift=\Hc cm]
%		\draw[thick] (0,0) ellipse ({\R} and {\Rk});
%	\end{scope}
%	% --- твірні циліндра ---
%	\draw[thick] (\R,0) -- (\R,\Hc);
%	\draw[thick] (-\R,0) -- (-\R,\Hc);
%
%	% --- поле B: наскрізні вертикальні стрілки ---
%	\foreach \x in {-1,0,1}{
%			\pgfmathsetmacro{\bx}{\x*0.62*\R}
%			\draw[dfield,-{Latex[length=2.6mm]}] (\bx,-0.9) -- (\bx,\Hc+1.1);
%		}
%	\node[dfield,above] at (0,\Hc+1.1) {$\Bfield$};
%
%	% --- поле E: радіальні стрілки на трьох висотах (передня видима півсфера) ---
%	\foreach \y in {0,0.5,1}{
%			\pgfmathsetmacro{\yh}{\y*\Hc}
%			\foreach \a in {200,250,290,340}{
%					\pgfmathsetmacro{\ex}{\R*cos(\a)}
%					\pgfmathsetmacro{\ey}{\yh+\Rk*sin(\a)}
%					\draw[efield,-{Latex[length=2mm]}] (0,\yh) -- (\ex,\ey);
%				}
%		}
%	\pgfmathsetmacro{\elx}{1.05*\R*cos(340)}
%	\pgfmathsetmacro{\ely}{0.5*\Hc+1.05*\Rk*sin(340)}
%	\node[efield,right] at (\elx,\ely) {$\Efield$};
%
%	% --- вектор Пойнтінга: циркуляція навколо осі (два радіуси, середня висота) ---
%	\pgfmathsetmacro{\midy}{0.5*\Hc}
%	\pgfmathsetmacro{\Ra}{0.55*\R}   \pgfmathsetmacro{\Rka}{0.55*\Rk}
%	\pgfmathsetmacro{\Rb}{0.92*\R}   \pgfmathsetmacro{\Rkb}{0.92*\Rk}
%	\pgfmathsetmacro{\Rcc}{1.18*\R}  \pgfmathsetmacro{\Rkc}{1.18*\Rk}
%	\begin{scope}[yshift=\midy cm]
%			\draw[pfield,-{Latex[length=2.2mm]}] (250:{\Ra} and {\Rka}) arc (250:600:{\Ra} and {\Rka});
%			\draw[pfield,-{Latex[length=2.2mm]}] (250:{\Rb} and {\Rkb}) arc (250:600:{\Rb} and {\Rkb});
%			\pgfmathsetmacro{\Plx}{\Rcc*cos(110)}
%			\pgfmathsetmacro{\Ply}{\Rkc*sin(110)}
%			\node[pfield] at (\Plx,\Ply) {$\vect\Pi$};
%		\end{scope}
%
%	% --- радіус R (уздовж верхнього еліпса) ---
%	\begin{scope}[yshift=\Hc cm]
%			\draw[<->] (0,0) -- node[above] {$R$} (\R,0);
%		\end{scope}
%\end{tikzpicture}



\end{document}

